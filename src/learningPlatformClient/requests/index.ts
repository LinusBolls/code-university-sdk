import {
  getAllEvents,
  getEventGroups,
  getOwnSettings,
  getUpcomingEvents,
} from './general';

export const LearningPlatformRequest = {
  getAllEvents,
  getEventGroups,
  getUpcomingEvents,
  getOwnSettings,
};
